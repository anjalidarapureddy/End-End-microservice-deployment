apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}-config
  labels:
    {{- include "backend.labels" . | nindent 4 }}
data:
  MONGO_URL: {{ .Values.config.mongoUrl }}
  REDIS_HOST: {{ .Values.config.redisHost }}
  KAFKA_BROKER: {{ .Values.config.kafkaBroker }}
